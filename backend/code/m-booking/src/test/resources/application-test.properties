# Used for testing purposes #

# Instructions to follow when switching to update are marked with '***'
spring.jpa.hibernate.ddl-auto=create
spring.datasource.url = jdbc:mysql://localhost:3306/mbooking_test?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username = root
spring.datasource.password = root

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

spring.jpa.database = MYSQL

# loads the data from test.sql when booting
# *** comment the below section when in update
spring.datasource.data=classpath:test.sql

# *** uncomment this section when in update mode
#spring.datasource.initialization-mode=never
